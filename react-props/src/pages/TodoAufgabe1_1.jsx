// Aufgabe:
// Dieses Mal sollst du in React eine To-Do-Liste nachbauen.
// Erstelle dir eine “Datenbank” (data mit Array und Objekten)
// Nutze props um den Text einzufügen!
// Hinweis: Erstelle eine Komponente TodoList & TodoItem, die ToDos werden mit props von der TodoList and die TodoItems weiter gegeben.

import TodoItem from "../components/TodoItem";

const TodoAufgabe1_1 = () => {
  return <TodoItem />;
};

export default TodoAufgabe1_1;
